# Chat2Calendar 个人 QQ 智能提醒助手项目规划

> 文档版本：v0.2
> 项目阶段：方案设计
> 适用范围：Windows 单用户个人工具

## 1. 项目概述

Chat2Calendar 当前阶段不接入 Meet 课表或其他日历软件，而是先实现一个完整的 QQ 内提醒闭环：程序通过 NapCat 监听用户指定的 QQ 会话，将新消息交给云端 AI 识别；识别出日程或待办后，通过 QQ 向用户本人发送确认消息；用户确认后，由 Windows 本地调度器保存任务，并在约定时间再次通过 QQ 提醒用户。

现有仓库名称继续使用 `Chat2Calendar`。日历写入能力作为后续扩展，本阶段只实现 QQ 消息采集、AI 识别、QQ 确认和定时提醒。

## 2. 已确认的产品决策

- 仅供用户本人使用，不建设多用户系统。
- 运行平台为 Windows。
- 使用 NapCat 登录用户的主 QQ。
- 通过 OneBot 11 接收和发送 QQ 消息。
- 只采集用户显式配置的私聊或群聊。
- 只处理程序在线期间收到的新消息，不读取或补抓历史聊天记录。
- 使用云端 AI 识别日程、待办和提醒信息。
- 一条消息可以产生零个、一个或多个独立候选事项。
- 识别出的候选事项必须先通过 QQ 发送给用户确认。
- 只有配置的所有者 QQ 号能在指定控制会话中执行确认、修改、忽略、取消和查看命令。
- 用户确认后才创建本地定时任务。
- MVP 中每个任务只有一个实际提醒时间；提前提醒会替代到期时提醒，不同时创建两个触发点。
- MVP 不支持周期性提醒。
- 到达提醒时间后，通过 QQ 再次提醒用户。
- 当前 MVP 只运行一个登录用户主 QQ 的 NapCat 实例。
- 提醒和控制目标仅支持该账号的同账号自聊或 QQ“我的电脑”，由阶段 0 选择其中一种验证通过的模式。
- QQ 发送采用有限次数重试的尽力送达语义；在“服务端可能已发送但客户端未收到响应”的情况下保留不确定状态并允许带同一任务 ID 的重复提醒，不能承诺必达或严格恰好一次。
- 当前阶段不写入 Meet 课表或其他日程软件。

## 3. 项目目标

### 3.1 核心目标

1. 稳定监听用户指定 QQ 会话中的实时新消息。
2. 准确识别消息中具有时间约束的日程、截止日期和待办事项。
3. 通过经过身份校验的 QQ 控制会话完成候选事项确认、修改和忽略操作。
4. 在 Windows 本地可靠保存和调度提醒任务。
5. 到达约定时间后，将提醒发送到用户自己的 QQ。
6. 在正常路径中避免重复提醒、误触发和系统消息循环，并让极端情况下的未知发送结果可追踪、可恢复。

### 3.2 非目标

首个版本不包含以下能力：

- 导入 QQ 历史聊天记录，或补抓程序离线期间的消息。
- 监听全部 QQ 会话。
- 为多个用户提供服务。
- 写入 Meet 课表、Outlook、Google Calendar 或其他日历。
- 处理图片、聊天截图、语音和文件内容。
- 周期性提醒和同一任务的多个提醒触发点。
- 未经确认自动创建、修改或取消提醒。
- 在 OneBot 不提供幂等和送达确认能力时承诺每条提醒必达或严格恰好一次送达。
- 多 NapCat、多发送账号、第二 QQ 私聊或群聊控制通道。
- 移动端常驻运行。
- Web 管理后台或复杂桌面客户端。

## 4. 用户使用流程

```text
指定QQ会话产生新消息
  -> NapCat推送OneBot消息事件
  -> 程序执行连接鉴权、白名单、消息方向和类型检查
  -> 将标准化事件以唯一键写入本地inbox
  -> 按会话顺序进入有界处理队列
  -> 读取当前消息及最小必要上下文
  -> 云端AI判断是否包含日程、待办或变更意图
  -> 程序校验Schema、关联关系和日期时间
  -> 在同一事务中保存候选版本并写入通知outbox
  -> 通过QQ向用户发送待确认消息
  -> 所有者在指定控制会话回复确认、修改或忽略
  -> 程序重新校验版本和时间，创建或放弃本地提醒任务
  -> Windows本地调度器等待触发
  -> 到达提醒时间后通过持久化outbox发送提醒
  -> 程序记录明确成功、明确失败或结果未知，并按策略重试
```

## 5. 典型使用场景

### 5.1 明确日程

原消息：

```text
周五下午三点在A栋会议室做项目评审。
```

系统识别：

- 事项：项目评审
- 时间：根据消息发送时间计算出的本周五 15:00
- 地点：A 栋会议室
- 状态：等待用户确认

### 5.2 截止日期

原消息：

```text
课程设计报告这周日晚八点前交。
```

系统识别：

- 事项：提交课程设计报告
- 截止时间：本周日 20:00
- 默认提醒时间：本周日 20:00
- 如果用户选择提前 30 分钟，实际提醒时间改为本周日 19:30，不再于 20:00 重复提醒

### 5.3 上下文修改

会话内容：

```text
甲：我们明天下午两点开会。
乙：改到四点吧。
```

系统需要结合最近的会话上下文，将会议时间识别为明天 16:00。如果 14:00 候选已经发出，则将旧版本标记为 `superseded` 并发送新版本；对旧版本的确认命令不得生效。如果原任务已经确认，改期只生成待确认变更，不能未经所有者确认直接修改已调度任务。

### 5.4 单条消息包含多个事项

原消息：

```text
周五下午三点评审，周日晚上八点前交报告。
```

系统分别生成“项目评审”和“提交报告”两个候选事项，每个事项拥有独立任务 ID 和确认状态。一个事项识别失败不能阻止同一消息中的其他有效事项进入确认流程。

### 5.5 信息不完整

原消息：

```text
下周找时间讨论一下。
```

由于缺少明确日期和时间，系统不创建任务，而是通过 QQ 询问用户补充具体时间。追问同样生成带有效期的候选版本，过期后必须重新识别或修改。

## 6. QQ 确认交互

### 6.1 待确认消息

```text
[Chat2Calendar:R-20260918-001][待确认 V1]

操作：创建提醒
事项：提交课程设计报告
截止时间：2026-09-20 20:00
提醒时间：2026-09-20 20:00
来源：软件工程课程群
原文摘录：报告这周日晚八点前交

可回复：
确认 R-20260918-001 V1
修改 R-20260918-001 V1 明晚九点
忽略 R-20260918-001 V1
```

任务 ID 用于标识同一业务事项，版本号用于阻止旧确认覆盖新修改。任务 ID 不作为身份凭证，也不依赖其不可猜测性保证安全。`update` 和 `cancel` 候选必须在“操作”字段中明确显示变更类型和目标任务当前摘要。

### 6.2 支持的控制命令

- `确认 <任务ID> <版本>`：仅当版本仍是当前待确认版本时，应用候选的创建、更新或取消意图。
- `修改 <任务ID> <版本> <新要求>`：解析标题、时间或提醒方式，生成下一版本并再次请求确认。
- `忽略 <任务ID> <版本>`：忽略当前候选版本。
- `取消 <任务ID>`：取消已经确认但尚未触发的提醒。
- `查看提醒`：列出未来尚未完成的提醒。
- `查看 <任务ID>`：查看指定任务详情。

`确认`、`忽略`、`取消`和`查看`使用本地规则解析，不调用云端 AI。来源消息被识别为 `cancel` 时，程序只创建版本化的待确认取消候选，所有者通过 `确认 <任务ID> <版本>` 后才原子取消目标；所有者直接输入 `取消 <任务ID>` 本身视为显式取消操作。`修改` 中的自然语言要求优先由本地规则解析；只有 `privacy.owner_modification_ai_enabled=true` 且用户已经确认云端政策时，规则失败后才把“新要求”和目标任务的最小摘要发给 AI，不上传控制会话上下文。开关关闭时返回本地可解析格式提示。待修改任务由命令中的任务 ID 固定，模型不能改选其他任务。重复命令必须幂等返回已有结果。

### 6.3 到期提醒消息

```text
[Chat2Calendar:R-20260918-001][提醒]

事项：提交课程设计报告
截止时间：今天 20:00
提醒时间：今天 20:00
来源：软件工程课程群
原文摘录：报告这周日晚八点前交
```

同一 delivery 的重试始终使用相同任务 ID；当上一次发送结果未知时，重试消息附加“可能与上一条重复”的提示。

### 6.4 命令入口和身份校验

- 配置唯一的 `owner_qq_id`、登录账号 `self_id` 以及一个控制会话。
- 控制会话可以与提醒目标相同，但不得同时作为普通 AI 来源会话；启动校验必须拒绝这种重叠配置。
- 每条命令在解析前校验 OneBot 事件方向、`self_id`、`user_id`、会话类型和会话 ID。
- 当前 MVP 的控制会话必须是阶段 0 验证通过的同账号自聊或“我的电脑”；任何群聊即使由 `owner_qq_id` 发出命令样式文本，也不能作为控制入口。
- 系统主动发送的事件先按 OneBot 事件元数据拦截，不进入命令解析器或 AI 流程。
- 鉴权失败只记录脱敏审计事件，不返回任务是否存在，防止信息泄露。
- 阶段 0 必须根据 NapCat 真实事件确认同账号自聊和“我的电脑”两种模式下可用于鉴权的字段；无法可靠鉴权的模式不得用于正式控制。

## 7. 功能需求

### 7.1 NapCat 消息接入

- 使用 OneBot 11 反向 WebSocket。
- Chat2Calendar 只在 `127.0.0.1` 启动 WebSocket 服务端，NapCat 主动连接。
- 在接受业务事件和动作响应前校验随机 Access Token；鉴权失败立即断开且不得记录 Token。
- 接收私聊、群聊、主动发送消息和元事件，并按 NapCat 实际字段区分方向。
- 同一 `self_id` 默认只允许一个活动连接；新旧连接冲突时记录状态并采用确定的替换策略。
- 记录连接、断开、重连和心跳状态，对尚未完成的 `echo` 请求标记连接中断。
- 连接断开后自动等待 NapCat 重连，不轮询或补抓历史聊天记录。
- 阶段 0 保存脱敏后的真实事件样本，覆盖私聊、群聊、同账号自聊、“我的电脑”、主动发送、动作响应和重连。

### 7.2 来源会话白名单

白名单条目至少包含：

- 会话类型：`private` 或 `group`。
- QQ 号或群号。
- 显示名称，仅用于界面和脱敏日志。
- 是否启用。
- 消息方向：只处理收到的消息，或处理会话双方消息。
- AI 分析模式：全部分析或本地预筛选后分析。
- 是否允许将该会话的最小必要内容上传至已确认的云端供应商。

任何不在来源白名单中的普通聊天消息都必须在写入正文和调用云端 AI 之前丢弃。来源白名单不授予控制权限；控制命令必须另外通过第 6.4 节的所有者校验。通过鉴权的控制命令不要求出现在来源白名单中，作为 `kind=command` 写入优先 inbox，并先由本地命令处理器消费；只有第 6.2 节定义的已授权 `修改` 载荷可以在独立隐私开关启用时调用 AI，其他命令和控制会话上下文绝不上云。系统主动发送事件不受“处理会话双方消息”配置影响，始终先进入防循环过滤。来源会话关闭 `cloud_upload_enabled` 时，不保存待上传正文或调用 AI，只记录不含正文的跳过计数。

### 7.3 消息标准化、inbox 和上下文

- 使用 `self_id + message_type + conversation_id + message_id` 组成入站事件唯一键，并在验证 NapCat 真实数据后补充必要的 `sub_type` 字段。
- 普通来源消息通过连接鉴权、来源白名单和基本类型检查，控制命令通过连接与所有者鉴权；只有通过对应准入规则的事件才写入 SQLite `inbound_messages`，任何 AI 调用都发生在落库之后。
- 入站状态至少包含 `received`、`analyzing`、`retry_scheduled`、`completed`、`failed`、`expired_privacy` 和 `ignored`；状态领取使用租约，程序重启后可恢复过期租约。
- AI 返回后提交候选时，必须以“入站状态仍为 `analyzing` 且正文尚未到期”为条件更新；条件失败时丢弃迟到的 AI 结果，不创建候选或通知。
- 保留 OneBot 消息 ID、消息发送时间、发送者、会话 ID、文本片段边界和回复引用关系。
- 第一版只处理纯文本及消息中的文本片段；图片、语音、文件和无法安全解析的 CQ 段标记为不支持，不将不同文本段无分隔拼接。
- 单条文本和单次上下文分别设置字符数、消息数和 Token 上限，超限时截断并记录原因。
- 每个会话只允许一个 AI 识别任务同时执行，保证“改到四点”等后续消息不会越过前序消息。
- 上下文只包含完成当前识别所需的最近消息。未处理正文到达保留期限时先原子转为 `expired_privacy`，再清除正文和停止后续识别；若 AI 调用正在进行，可以取消调用或等待返回，但返回结果必须因条件更新失败而丢弃。已处理消息的正文按同一短 TTL 清理，候选仅保留必要摘录。
- OneBot 反向 WebSocket 没有离线补抓保证，因此程序未连接期间以及事件落库前发生的丢失不计入“可靠恢复”承诺，并应在运行状态中明确显示离线区间。

### 7.4 AI 识别

云端 AI 必须返回符合固定 Schema 的 JSON，不允许程序依赖自然语言回答或接受 Schema 之外的额外字段。

固定顶层格式示例：

```json
{
  "results": [
    {
      "intent": "create",
      "target_reference": null,
      "evidence_quote": "报告这周日晚八点前交",
      "item": {
        "kind": "deadline",
        "title": "提交课程设计报告",
        "due_at": "2026-09-20T20:00:00+08:00",
        "remind_at": "2026-09-20T20:00:00+08:00",
        "location": null,
        "confidence": 0.96,
        "missing_fields": []
      }
    }
  ]
}
```

顶层对象只允许一个有数量上限的 `results` 数组；空数组表示没有事项。数组中的每个结果使用 `intent` 作为 Pydantic 判别字段并独立校验：

- `create`：`target_reference` 必须为 `null`，`item` 是一个完整事项，`due_at`、`remind_at` 非空且 `missing_fields` 为空。
- `update`：`target_reference` 必须唯一引用本次请求中提供的候选或任务，`item` 是合并旧值后的一个完整替代事项。MVP 每个结果只更新一个目标。
- `cancel`：`target_reference` 必须唯一引用允许取消的目标，`item` 必须为 `null`。
- `clarify`：`item` 是一个草稿事项，至少有一个 `missing_fields`，缺失的 `due_at` 或 `remind_at` 明确为 `null`；`target_reference` 可以为空或引用一个允许的现有目标。
- `kind` 只能是 `event`、`deadline` 或 `todo`。
- 所有 `target_reference` 都必须来自程序随本次请求提供的 allowlist；程序再次校验来源会话、版本和当前状态。
- MVP 不接受 `recurrence` 字段；识别到周期规则时返回一个 `clarify` 结果并明确提示暂不支持。
- 每个结果都有独立的 `evidence_quote`，必须能在本次消息或允许的上下文中找到；该字段位于结果级，因此 `item=null` 的 `cancel` 仍能保存取消证据。证据摘录、标题和地点均有长度限制。
- 置信度只用于决定直接发候选还是追问，不能绕过程序校验和用户确认。
- 顶层结构或数量上限无效时拒绝整个响应；单个结果不满足对应 intent 时只拒绝该结果并记录原因，其他有效结果继续处理。
- 同一响应不得对同一 `target_reference` 给出多个变更结果。完整 `create` 与不完整 `clarify` 可以同时出现，从而分别生成候选确认和补充信息追问。

识别范围包括会议、约会、课程、活动、截止日期、未来待办，以及对同一会话中已有事项的改期或取消表达。聊天内容全部视为不可信数据；提示词必须明确忽略其中要求模型泄露上下文、改变 Schema 或执行命令的内容。AI 只能提出候选变更，不能直接创建、修改、取消任务或决定命令授权。

### 7.5 时间校验

- 展示时区默认为 `Asia/Shanghai`，数据库内部统一保存带时区时间并规范化为 UTC。
- “今天”“明天”等表达以原消息发送时间为基准，不以 AI 调用时间或程序恢复时间为基准。
- 一周按周一至周日计算；“本周五”固定指当前自然周，“下周五”指下一自然周，裸“周五”取不早于消息时间的最近一次。解析到过去时间时必须追问，不能静默滚到下一周。
- `due_at` 表示事件开始或截止时间，`remind_at` 表示唯一实际触发时间。设置提前量时，`remind_at = due_at - advance`。
- 缺少日期或具体时间时不使用隐藏默认值，而是通过 QQ 追问。
- 校验日期是否存在、时区偏移是否正确、提醒是否早于当前时间，以及时间是否超过可配置的最大未来期限。
- 候选默认在创建后 24 小时过期，实际 `expires_at` 取该时间与 `remind_at` 中较早者。
- 确认和修改必须在数据库事务内重新校验候选版本、`expires_at` 和 `remind_at`；过期候选进入 `expired`，不得被调度。
- 更新或取消现有事项时必须唯一关联到允许的候选或任务。关联不明确时只追问，不猜测目标。
- AI 输出通过 Pydantic Schema 后仍需执行上述业务规则，任何字段都不能直接信任。

### 7.6 本地调度

- SQLite 是提醒和发送状态的唯一事实来源；APScheduler 只负责进程内唤醒，不单独持久化业务状态，启动时从 SQLite 重建任务。
- 用户确认且事务提交后，提醒才进入 `scheduled`。
- 调度时间使用带时区时间，比较和领取任务时使用 UTC。
- 到期任务先通过条件更新原子领取并写入发送 outbox，再执行网络发送，防止多个执行器同时触发。
- 程序启动时恢复 `scheduled`、租约已过期的 `firing`、`retrying` 和 `unknown` 记录，而不只加载未来任务。
- 对程序退出或 Windows 休眠期间到期的任务，在 `misfire_grace_minutes` 内立即补发并标注“延迟提醒”；超过宽限期则标记为 `missed`。
- 取消操作与任务领取使用条件更新解决竞态：取消成功后不得新建发送意图；已经进入发送中的任务则返回“可能已发送”的明确结果。
- MVP 每个任务只注册一个 `remind_at`，不创建周期任务或多个触发点。

### 7.7 QQ 消息发送

当前 MVP 只使用登录主 QQ 的单个 NapCat 实例，并在同账号自聊与“我的电脑”中选择一个通过阶段 0 验证的提醒和控制目标。使用 OneBot 发消息动作，并通过每次尝试唯一的 `echo` 关联请求与响应。

正式开发前必须验证：

1. NapCat 能否向当前登录账号自身发送消息。
2. 能否向“我的电脑”稳定发送消息。
3. 用户在该会话中的回复能否产生可鉴权的 OneBot 消息事件。
4. 主动发送事件与动作响应的先后顺序及字段是否稳定。
5. 发送成功后能否获得消息 ID，以及响应丢失后是否存在可靠查询方式。
6. QQ 重启、NapCat 重连或重新登录后目标标识是否保持稳定。
7. 错误或缺失 Access Token 是否会被可靠拒绝。

如果同账号自聊和“我的电脑”都无法稳定发送、接收回复并完成可靠鉴权，则阶段 0 失败并暂停后续开发。第二 QQ、专用提醒群和 Windows 通知都需要重新确定账号、网关和隐私模型，属于后续扩展，不作为当前 MVP 的临时降级。

### 7.8 队列、并发和成本控制

- `queue_capacity` 只限制内存中的活动工作项；待处理 inbox 另设 `max_pending_inbox` 和 `max_database_mb` 两个硬上限。
- 达到任一持久化硬上限时进入 `overloaded`：停止接收新的来源消息正文并丢弃最新来源事件，只累计不含正文的丢弃数量和起止时间，同时保留专用容量处理所有者本地命令。
- 恢复到各自低水位后退出 `overloaded`，向所有者报告明确的漏识别区间；系统不得声称该区间消息已可靠接收。
- 正常状态下达到内存队列上限时，保留已持久化 inbox 并暂停领取，不在内存中无限堆积。
- 同一会话严格串行，不同会话受全局并发上限控制。
- AI 超时、限流和无效 JSON 使用有上限的指数退避并加入抖动，不阻塞其他会话。
- 配置每分钟请求上限、每日 Token 或费用软上限；触发上限后停止新的 AI 调用并记录可见状态。
- 本地预筛选只能决定是否调用 AI，规则和误判率必须通过固定测试集验证。
- 进程正常关闭时停止领取新消息，并在超时范围内完成或释放正在处理的租约。

### 7.9 持久化 outbox 和送达语义

- 候选确认、到期提醒和错误通知先写入 `outbox`，事务提交后才能发送。
- 每个逻辑通知使用稳定 `delivery_id`，每次网络尝试另有唯一 `attempt_id` 和 `echo`。
- outbox 状态至少包括 `pending`、`sending`、`retry_scheduled`、`delivered`、`failed`、`unknown`、`unknown_exhausted` 和 `obsolete`。
- DeliveryService 在一个短事务中领取 `pending/retry_scheduled` 记录、写入 `lease_owner/lease_expires_at`，并创建状态为 `prepared` 的 attempt。
- 候选失效事务可以把 `pending`、`retry_scheduled`，以及 `sending` 且当前 attempt 的 `submitted_at IS NULL` 的通知原子改为 `obsolete`。
- 交给 WebSocket 前，在同一短事务中再次校验关联候选版本、任务状态、outbox 仍为 `sending` 且 `submitted_at IS NULL`，然后写入 `submitted_at`。候选失效与提交网络的竞态由这两个互斥条件更新决定，任一方只能成功一次。
- 租约过期后，没有 `submitted_at` 且未被作废的 attempt 可安全回到 `pending`；已有 `submitted_at` 但没有响应的 attempt 必须进入 `unknown`，即使它可能尚未真正到达 NapCat。
- 明确失败按配置执行有限次数退避重试，耗尽后标记 `failed`；未知结果也有限次重试，耗尽后标记 `unknown_exhausted`，不能伪装成确定失败或成功。
- 未知结果重试可能产生带同一任务 ID 的重复消息；系统通过稳定 ID 和提示让用户识别，而不宣称端到端恰好一次。
- `echo` 和返回消息 ID 只用于关联、审计和正常路径去重，不能证明最终送达。

## 8. 系统架构

```text
Windows QQ
    |
NapCat / OneBot 11
    | Reverse WebSocket
NapCatGateway
    |
EventGate（连接鉴权 / 来源白名单 / 命令鉴权 / 防循环）
    |
SQLite inbox -> 有界队列 -> ContextBuffer -> AIExtractor
                                      |
                               CandidateService
                                      |
                              SQLite outbox
                                      |
ReminderScheduler -> DeliveryService -> QQNotifier -> NapCatGateway
```

SQLite 保存所有需要跨重启恢复的业务状态。内存只保存连接、短期上下文缓存、队列消费者和由数据库重建的调度句柄，不能成为唯一事实来源。

## 9. 模块划分

### 9.1 `NapCatGateway`

- 接收 OneBot 消息和元事件。
- 调用 OneBot 发消息动作。
- 管理连接鉴权、单活动连接、心跳、断线和 `echo` 请求映射。
- 将断线时未完成的动作标记为结果未知，不自行决定业务重试。

### 9.2 `EventGate`

- 加载来源会话白名单和控制会话配置。
- 根据事件元数据、系统标记和发送中的 outbox 联合判断消息方向及是否为系统主动发送，不能把同账号的所有出站消息一律过滤。
- 对控制命令校验 `self_id`、`owner_qq_id` 和控制会话。
- 在保存正文或上传云端前阻止未授权消息。

### 9.3 `InboxWorker`

- 幂等写入标准化入站事件。
- 使用租约领取消息，按会话串行处理并限制全局并发。
- 处理失败重试、过期租约恢复、队列背压和正常关闭。
- 在事务中提交处理结果、候选事项和通知 outbox。

### 9.4 `ContextBuffer`

- 按会话维护最小必要短期上下文。
- 保留文本片段和回复关系，控制消息数、字符数和 Token 数。
- 超时或达到长度限制后清理旧消息，不承担持久化职责。

### 9.5 `AIExtractor`

- 对接固定且经过允许的 OpenAI 兼容云端 API。
- 强制结构化输出并拒绝额外字段。
- 处理超时、限流、重试和无效 JSON。
- 记录模型、提示词版本、耗时和 Token 用量，不记录 API Key，且默认不记录完整正文。

### 9.6 `CandidateService`

- 为单条消息中的每个事项创建独立任务 ID 和候选版本。
- AI 识别出的取消意图保存为待确认候选；确认时只有目标提醒仍处于允许取消的状态才执行条件更新，否则候选进入 `conflict` 且不改变当前任务。
- 使用版本条件更新阻止重复命令、旧确认和并发修改。
- 修改已调度任务时，确认事务通过条件更新把仍为 `scheduled` 的旧提醒改为 `cancelled`，并创建新提醒。
- 如果旧提醒已进入 `firing`、终态或状态未知，则不修改旧任务或创建新提醒，把改期候选标记为 `conflict`，并向所有者返回“旧提醒可能已发送，改期未应用”；后续新时间需作为独立候选再次确认。

### 9.7 `ReminderScheduler`

- 以 SQLite 为事实来源，创建可重建的进程内定时句柄。
- 原子领取到期任务，处理取消竞态、过期租约、休眠补发和错过状态。
- 只负责生成发送意图，不直接执行网络发送。

### 9.8 `DeliveryService` 与 `QQNotifier`

- 从 outbox 领取通知并记录每次发送尝试。
- 生成统一的确认、追问、延迟、重复风险和提醒文本。
- 调用 NapCat 发送消息，将响应映射为成功、失败或未知。
- 按有限次数、未知结果可能重复的尽力送达策略安排重试。

### 9.9 `LocalStore`

- 管理 SQLite Schema、迁移、外键、唯一约束和事务。
- 保存 inbox、候选、提醒、outbox、发送尝试和状态变更。
- 配置 WAL、`busy_timeout`、数据保留、清理和一致性备份。

## 10. 状态模型

入站消息：

```text
received -> analyzing -> completed
received -> ignored
received / retry_scheduled -> expired_privacy（正文保留期耗尽）
analyzing -> expired_privacy（处理跨过正文保留期，丢弃迟到结果）
analyzing -> retry_scheduled -> received
analyzing -> failed（超过重试上限）
analyzing（租约过期） -> received
```

候选事项：

```text
pending_confirmation
  +-> confirmed
  +-> ignored
  +-> expired
  +-> conflict（目标状态已变化）
  +-> superseded -> pending_confirmation（新版本）
```

提醒任务：

```text
scheduled -> cancelled
scheduled -> missed
scheduled -> firing
firing -> delivered
firing -> retrying -> firing / failed
firing -> unknown -> retrying / unknown_exhausted
```

outbox：

```text
pending / retry_scheduled -> obsolete
sending（当前 attempt 尚未提交） -> obsolete
pending / retry_scheduled -> sending
sending -> delivered / retry_scheduled / failed / unknown
sending（租约过期且未提交） -> pending
sending（租约过期且已提交） -> unknown
unknown -> retry_scheduled / unknown_exhausted
```

实现时不得依赖图中的缩进推断事务规则：每个状态都要定义允许的来源状态、条件更新语句、租约期限和启动恢复动作。每次状态变化追加记录时间、原因和关联 attempt ID，不能只覆盖当前状态。

## 11. 建议数据表

### 11.1 `conversations`

保存来源白名单会话类型、QQ 号或群号、显示名称、消息方向、分析模式、云端上传开关和启用状态。控制会话及所有者身份单独配置，不能由来源白名单隐式推导。

### 11.2 `inbound_messages`

保存复合事件键、事件种类、最小标准化消息、处理状态、租约、尝试次数、错误类别和处理时间。复合事件键必须有唯一约束；短期正文到期后清空，但保留去重键和处理结果摘要。超过正文保留期仍未完成的消息必须先转为 `expired_privacy`，不能保留一个已无法继续处理的非终态。

### 11.3 `candidates`

保存任务 ID、版本、AI 识别结果、必要来源摘录、置信度、`expires_at`、状态及取代关系。`task_id + version` 唯一，同一任务最多只有一个当前待确认版本。

### 11.4 `reminders`

保存已确认任务、`due_at`、唯一 `remind_at`、状态、领取租约、重试计划时间和最后结果。确认候选与创建提醒在同一事务中完成。

### 11.5 `outbox`

保存稳定 `delivery_id`、通知类型、任务 ID、目标会话、可重建的模板数据、状态、`lease_owner`、`lease_expires_at` 和下次尝试时间。业务状态和 outbox 写入必须位于同一事务；候选失效时，同一事务将 `pending/retry_scheduled` 或 `sending` 且尚无 `submitted_at` 的关联通知标记为 `obsolete`。

### 11.6 `delivery_attempts`

保存每次 `attempt_id`、`echo`、连接 ID、`prepared_at`、`submitted_at`、响应时间、经过字段筛选的 OneBot 响应、返回消息 ID、错误分类和最终结果。请求头、Token、API Key 和响应中的非必要正文不得写入。

### 11.7 `state_transitions`

追加保存实体类型、实体 ID、原状态、新状态、时间、原因和关联操作，用于崩溃恢复审计和问题定位。

### 11.8 `settings`

保存非敏感运行配置和 Schema 版本。NapCat Token 和云端 AI API Key 不以明文写入该表。

数据库至少启用外键、必要唯一索引、WAL 和合理的 `busy_timeout`。所有 Schema 变更使用可测试迁移，不在启动时执行不可回滚的临时改表逻辑。

## 12. 防循环和防重复设计

- 系统消息统一包含 `[Chat2Calendar:<任务ID>]` 标记；没有单一任务 ID 的状态和列表消息使用 `[Chat2Calendar:SYSTEM]`。标记用于辅助识别和人工排查，不作为权限凭证。
- 同账号模式下，事件方向不能区分用户手动输入和程序主动发送。程序发送前已有 outbox，收到出站事件时使用系统标记、目标会话、规范化载荷指纹和短时间窗口匹配 `sending` attempt；匹配成功才作为系统事件拦截。
- 动作响应返回后补充保存消息 ID，后续重放事件按消息 ID 直接忽略。若未匹配到 outbox，且消息来自经过鉴权的控制会话并符合命令语法，则允许进入本地命令解析器。
- 匹配逻辑必须覆盖主动发送事件早于动作响应的情况，同时避免把用户手动输入的确认、修改或取消命令当作系统事件。
- 经过所有者鉴权的控制命令进入独立命令解析器，不进入普通聊天 AI 识别流程；可选的 `修改` AI 回退只能遵循第 6.2 节的独立隐私开关和最小载荷规则。
- 入站复合事件键是第一层幂等约束。
- 内容指纹只在同一账号、会话、发送者和短时间窗口内辅助检测重放，不能跨会话或长期阻止内容相同的合法消息。
- 业务重复检测必须区分“同一事项的更新”和“内容相似的两个真实事项”；无法确定时向用户展示候选，不自动合并。
- 测试必须覆盖主动发送事件早于动作响应、用户手动输入与程序出站事件区分、重连重放、重复命令、同文本合法重复以及取消与触发并发。

## 13. 安全与隐私

- NapCat 和 Chat2Calendar 仅监听 `127.0.0.1`。
- OneBot WebSocket 必须配置高熵随机 Access Token，使用常量时间比较，并拒绝缺失或错误 Token。
- 只有配置的 `owner_qq_id` 能在指定控制会话执行命令；来源白名单不等于命令权限。
- 云端 AI API Key 使用 Windows Credential Manager 或 DPAPI 保存。
- AI 端点必须使用 HTTPS，并通过配置限制到用户明确选择的服务地址。
- 启用云端识别前，用户必须确认会上传白名单会话的必要内容；每个来源会话可以独立关闭云端上传。
- 云端供应商准入记录至少包含供应商名称、数据政策链接、复核日期、保留期限、是否用于训练及删除方式。优先选择支持零保留或明确禁止训练的服务；政策不满足要求时不得启用。
- 本地删除只清除本机副本，不能声称同步删除供应商日志；文档必须说明供应商侧删除入口和能力边界。
- 通过鉴权的所有者 `修改` 命令只有在独立开关启用时，才能向 AI 提交新要求和目标任务最小摘要；其他命令及控制会话上下文不得上传。
- 聊天内容一律视为不可信输入；模型输出不能控制工具、配置、目标 QQ 或权限判断。
- 日志默认不记录完整聊天正文，隐藏 Token、API Key、完整鉴权请求头和不必要的 QQ 号。
- 未进入来源白名单的普通聊天消息不得持久化正文或上传云端；唯一例外是第 6.2 节中由所有者显式开启的 `修改` AI 回退，其仍只上传最小载荷。
- 只向 AI 提交识别所需的最小上下文，并在本地记录模型和提示词版本以便复现。
- 原始入站正文默认保留不超过 24 小时；到期仍未处理的消息先进入 `expired_privacy` 再清除正文。候选来源摘录默认保留 30 天；已完成 outbox 载荷默认保留 30 天；筛选后的动作响应默认保留 7 天；去重键、状态和非敏感统计可保留更久。
- 候选摘录过期后，未来提醒省略“原文摘录”字段，不因缺少摘录而阻止任务发送。
- 默认一致性备份是逻辑备份，不包含原始入站正文、候选摘录、已完成发送载荷或动作响应；包含敏感字段的可选备份必须加密并遵循不长于源字段的年龄上限。
- TTL 清理保留不含正文的审计结果；任何非终态 inbox、提醒或 outbox 记录不得因 TTL 被删除。
- SQLite 数据目录和备份限制为当前 Windows 用户访问，提供本地导出、清理和完整删除功能。
- NapCat 属于非官方 QQ 接入方式，需接受账号风控和版本兼容风险。

## 14. 异常处理

### 14.1 NapCat 断线

- 显示离线起止时间并等待自动重连。
- 不补抓断线期间的历史消息，并向用户明确该时间段可能漏识别。
- 已写入 inbox 的消息和已确认提醒继续保留。
- 断线时尚未收到响应的发送尝试标记为 `unknown`，交由 DeliveryService 处理。

### 14.2 云端 AI 不可用

- 对 inbox 消息执行有限次数退避重试，不阻塞其他会话。
- 超过重试上限后标记 `failed`，保留脱敏错误和人工重试入口。
- 连续故障触发熔断和本地状态提示，避免通过不可用的 QQ 通道反复发送错误通知。

### 14.3 QQ 提醒发送失败或结果未知

- 明确失败时保留 outbox 并按配置执行有限次数退避重试。
- 明确失败耗尽重试后标记 `failed`，不把任务伪装成已送达。
- 结果未知时重试可能产生重复消息；未知重试耗尽后标记 `unknown_exhausted`，保留“可能已发送”的事实。
- Windows 通知不属于当前 MVP；只有重新确定产品范围后才能作为独立兜底通道实施。

### 14.4 程序异常退出

- 网络调用前必须已经持久化业务状态和发送意图。
- 下次启动时释放过期租约并恢复 inbox、调度任务、outbox 和重试记录；outbox 根据 attempt 是否已有 `submitted_at` 分别回到 `pending` 或进入 `unknown`。
- 每个关键事务边界都通过崩溃注入测试验证，确保状态可解释且可继续处理。

### 14.5 SQLite 不可写、锁定或磁盘空间不足

- inbox 无法提交时不得调用 AI 或生成内存候选，健康状态标记为不可用。
- 达到待处理数量或数据库大小硬上限时进入 `overloaded` 并按第 7.8 节丢弃新来源正文，不能继续无界增长。
- 使用有限 `busy_timeout` 和重试，超过上限后停止领取新事件并记录本地错误。
- 备份和迁移前检查空间，失败时保留原数据库并终止升级。

### 14.6 Windows 休眠和系统时钟变化

- 唤醒后按 `misfire_grace_minutes` 执行延迟提醒或标记 `missed`。
- 内部比较使用 UTC；展示时重新转换到配置时区。
- 检测显著时钟跳变后重新扫描数据库，而不是继续依赖旧的内存等待时间。

## 15. 推荐技术栈

- Python 3.12
- `asyncio`：统一管理 WebSocket、队列消费者和关闭流程
- `websockets`：接收 NapCat 反向 WebSocket
- `Pydantic 2.x`：配置及 AI 输出严格校验
- `APScheduler`：进程内定时唤醒，不使用其 JobStore 作为业务事实来源
- `SQLite` 与 `aiosqlite`：本地持久化和显式事务
- OpenAI 兼容客户端：接入云端 AI
- Windows Credential Manager 或 DPAPI：保存密钥
- `pytest`、`pytest-asyncio` 和可控时钟工具：自动化测试
- Windows 任务计划程序：用户登录后启动和异常后重启
- `PyInstaller`：后续打包为 Windows 可执行程序

依赖项放入 `pyproject.toml` 并提交锁文件。阶段 0 确认 NapCat、QQ、OneBot、Python 和关键库的实际版本；之后固定主版本，升级必须重新运行协议和恢复测试。APScheduler 3.x 与 4.x API 差异较大，实施前必须选定一个主版本，不能使用无上限版本范围。

首个版本采用配置文件加命令行状态输出，不立即开发复杂 GUI。核心闭环稳定后再增加托盘图标和本地配置界面。Windows 任务计划程序只负责拉起 Chat2Calendar；QQ 和 NapCat 仍依赖用户登录会话，不能把“Windows 已启动”等同于通知通道已在线。

## 16. 配置示例

以下仅表示配置结构，实际 Token 和 API Key 不写入文件。QQ 号和群号统一按字符串处理。

```yaml
napcat:
  reverse_ws_host: 127.0.0.1
  reverse_ws_port: 8765
  expected_self_id: "填写NapCat登录QQ号"
  access_token_source: windows_credential_manager

owner:
  qq_id: "填写唯一所有者QQ号"
  control_conversation:
    type: private
    id: "可行性测试后填写"

source_conversations:
  - type: group
    id: "填写群号"
    name: "软件工程课程群"
    direction: incoming
    analysis_mode: all_messages
    cloud_upload_enabled: true
    enabled: true

notification:
  target_mode: self_or_my_computer
  target_type: private
  target_id: "可行性测试后填写"
  default_advance_minutes: 0
  retry_count: 3
  unknown_retry_delay_seconds: 30
  delivery_lease_seconds: 30

candidate:
  ttl_hours: 24
  max_future_days: 365

scheduler:
  misfire_grace_minutes: 360
  lease_seconds: 60

processing:
  queue_capacity: 500
  max_pending_inbox: 5000
  pending_low_watermark: 4000
  owner_command_reserve: 50
  max_concurrent_conversations: 3
  max_message_characters: 4000
  max_context_characters: 12000
  max_context_messages: 10
  max_context_tokens: 4000

ai:
  protocol: openai_compatible
  base_url: "待选择的HTTPS服务地址"
  model: "待选定并固定版本"
  api_key_source: windows_credential_manager
  timezone: Asia/Shanghai
  timeout_seconds: 15
  retry_count: 2
  candidate_confidence_threshold: 0.80
  requests_per_minute: 20
  daily_token_soft_limit: 100000

privacy:
  cloud_upload_acknowledged: false
  owner_modification_ai_enabled: false
  provider_name: "待确认"
  provider_policy_url: "待确认"
  provider_policy_reviewed_on: "YYYY-MM-DD"
  provider_deletion_method: "待确认"
  provider_deletion_url: "待确认或明确写not_supported"
  provider_retention_days: 0
  provider_training_opt_out: true

storage:
  database_path: data/chat2calendar.db
  max_database_mb: 512
  database_low_water_mb: 450
  retain_raw_inbound_hours: 24
  retain_source_excerpt_days: 30
  retain_completed_delivery_days: 30
  retain_delivery_response_days: 7
  retain_log_days: 14
  backup_count: 3
  backup_max_age_days: 30
  backup_include_sensitive_fields: false
```

程序启动时必须完整校验配置：端口、QQ 标识、`owner.qq_id` 与 `napcat.expected_self_id` 相同、控制会话为私聊且不与来源会话重叠、时间范围、队列与数据库硬上限、HTTPS AI 地址、云端上传确认及供应商政策记录任一无效时，先报告本地错误，不建立外部连接或处理消息。`cloud_upload_acknowledged` 必须由用户在阅读所选供应商政策后显式改为 `true`；`owner_modification_ai_enabled` 可以保持 `false`，且不得隐式开启。

## 17. 开发计划

### 阶段 0：技术可行性验证，2-3 天

交付内容：

- 编写最小 NapCat 探针，接收一个指定来源会话的新消息。
- 分别验证同账号自聊和“我的电脑”的发送、回复及操作者字段。
- 验证正确、缺失和错误 Access Token 的连接结果。
- 记录程序主动发送与用户手动命令的事件差异、动作响应顺序、消息 ID、`echo`、`self_id`、`user_id` 和会话字段。
- 模拟动作已提交后立即断线，确认是否存在发送结果查询能力。
- 验证 QQ/NapCat 重启、重连及多连接冲突。
- 保存脱敏事件 fixture，并记录 QQ、NapCat、OneBot 和操作系统版本。

退出条件：同账号自聊或“我的电脑”中至少一种模式能够稳定收取提醒、可靠鉴权所有者命令且不向其他人泄露任务内容；未授权连接和操作者可被识别并拒绝；发送结果未知时的能力边界已有实测记录。如果两种模式都无法可靠闭环，则阶段 0 失败并暂停后续开发，另行评估双账号或 Windows 通知方案。

### 阶段 1：可靠消息接入，3-4 天

交付内容：

- 项目骨架、严格配置校验和凭据读取接口。
- NapCatGateway 与 EventGate。
- SQLite Schema、迁移、inbox、唯一约束和状态租约。
- 有界队列、按会话串行和短期上下文。
- 模拟 OneBot WebSocket 与断线恢复测试。

退出条件：非白名单正文不落库且不进入处理队列；重复事件只产生一条 inbox 记录；任一处理步骤崩溃后，重启能够继续或进入可解释终态。

### 阶段 2：AI 识别，3-5 天

交付内容：

- 云端 AI 适配器和固定提示词版本。
- `create/update/cancel/clarify` 逐项判别 Schema，空 `results` 表示无事项。
- 单消息多事项、中文相对时间和目标关联校验。
- 缺失信息检测、输入输出边界和提示注入防护。
- 固定评测集、准确率报告、限流和成本统计。

退出条件：固定模型和提示词在第 18.5 节数据集上达到分类、时间和关联指标；非法 JSON、额外字段、越权目标及超时均进入确定的失败路径。

### 阶段 3：QQ 确认闭环，3-4 天

交付内容：

- 待确认、追问和任务列表模板。
- 所有者命令鉴权及确认、修改、忽略、取消和查看命令。
- 候选版本、过期、取代和重复命令幂等处理。
- 确认通知 outbox 和系统消息防循环机制。

退出条件：未授权操作者无法读取或改变任务；旧版本确认不能生效；AI 产生的取消候选可经版本化确认安全落地；目标状态变化时更新或取消候选进入 `conflict`；单条消息的多个候选可独立处理；程序出站事件和用户手动命令可可靠区分且不会形成消息循环。

### 阶段 4：可靠定时提醒，3-4 天

交付内容：

- SQLite 主导的本地调度器和原子任务领取。
- 持久化 outbox、delivery attempts、发送租约和有限次数的尽力重试。
- 程序重启、异常退出、Windows 休眠和时钟跳变恢复。
- 取消与触发竞态、明确失败和发送结果未知处理。

退出条件：所有非终态都有经过测试的启动恢复动作；宽限期内的过期任务会延迟补发，超期任务标记 `missed`；取消成功后不会创建新的发送意图；结果未知不会被错误标记为成功或明确失败；重试耗尽后保留 `failed` 与 `unknown_exhausted` 的区别。

### 阶段 5：Windows 交付，2-3 天

交付内容：

- 配置向导或简易本地配置界面。
- Windows 可执行文件和用户登录后启动任务。
- 运行状态、脱敏轮转日志、数据清理和一致性备份。
- 数据库迁移、升级回滚说明及安装使用文档。

退出条件：在一台干净的受支持 Windows 环境完成安装、配置、重启、升级和卸载验证；卸载或数据清理不会遗留密钥和聊天正文。

预计个人 MVP 总工期为 4-5 周，不包含 NapCat 或 QQ 版本发生阻断性变化后的等待和适配时间。

## 18. 测试计划

### 18.1 单元测试

- 配置边界、来源白名单和所有者命令鉴权。
- OneBot 消息标准化、文本段边界和复合事件键。
- 控制命令解析、重复命令和候选版本条件更新。
- AI `results[]` 逐项判别联合 Schema、混合完整与待补充事项、越权目标和字段长度限制。
- 相对时间、周边界、过期确认、提前量和最大未来期限。
- inbox、候选、提醒、outbox 和 delivery 状态流转。
- 内容指纹作用域、系统消息识别和调度任务重建。

### 18.2 集成与故障注入测试

- 使用模拟 OneBot WebSocket 测试正确和错误鉴权、收发消息、并发连接及重连。
- 测试主动发送事件早于、晚于和缺失动作响应三种顺序。
- 使用模拟 AI 服务测试超时、限流、非法 JSON、额外字段和重试熔断。
- 在“inbox 写入后”“AI 返回后”“候选与 outbox 提交后”“attempt prepared 后”“`submitted_at` 提交后”“OneBot 响应保存前”等边界强制终止进程，并验证租约恢复后分别进入安全重试或结果未知。
- 使用可控时钟测试触发、取消竞态、已调度任务改期竞态、休眠、跨日、时钟跳变和 misfire 宽限。
- 使用临时数据库测试唯一约束、锁定、磁盘写入失败、备份和逐版本迁移。
- 测试内存队列满载、持久化积压与数据库硬上限、过载区间记录、所有者命令保留容量、按会话顺序、全局并发和正常关闭释放租约。
- 测试处理中正文跨过 TTL 时进入 `expired_privacy`，并拒绝迟到 AI 结果创建候选。
- 测试候选忽略、过期或取代时，`pending/retry_scheduled` 以及 `sending` 但尚未写入 `submitted_at` 的旧 outbox 原子进入 `obsolete`，不再发送。

### 18.3 安全与隐私测试

- 非白名单会话、非所有者操作者、错误 `self_id`、错误控制会话、群聊命令和重放命令均被拒绝。
- 未授权消息正文不会进入数据库、日志或 AI 请求。
- 聊天中的提示注入不能改变 Schema、目标 QQ、任务 ID 或权限结果。
- 日志、错误信息、导出和备份中不包含 Token、API Key 或超出保留策略的正文。
- 未确认云端上传或供应商政策记录不完整时，AI 连接不会启动；文档明确本地删除与供应商侧删除的边界。
- `owner_modification_ai_enabled=false` 时任何控制命令都不上云；启用后也只有通过鉴权的 `修改` 新要求和目标最小摘要可以上传，控制会话上下文及其他命令仍被拒绝。
- TTL 清理后已完成正文与摘录被删除，超龄未处理正文进入 `expired_privacy`，幂等键和非敏感状态仍可正常使用。

### 18.4 端到端测试

- 指定 QQ 会话发送明确日程并收到待确认消息。
- 单条消息包含多个事项时分别确认或忽略。
- 回复确认后，在测试时间收到 QQ 提醒。
- 修改前后版本、来源消息取消候选、直接取消、迟到确认和查看命令行为正确。
- 重复事件和系统主动发送事件不会再次触发 AI。
- 非白名单消息不会调用云端 AI。
- 程序重启及 Windows 唤醒后按策略恢复提醒。
- QQ 发送明确失败、结果未知或未知重试耗尽时，任务状态和重试提示符合设计。

### 18.5 AI 固定评测集

评测集至少包含 130 条冻结样本：60 条单事项且日期时间明确的 `create`、30 条期望空 `results` 的普通负例、20 条上下文 `update/cancel`、10 条多结果消息、10 条单项信息不完整的 `clarify`。多结果样本中至少 5 条同时包含完整 `create` 和不完整 `clarify`。每条样本固定消息发送时间、时区、上下文及无序期望结果集，包括 intent、事项、目标引用、证据摘录、缺失字段和绝对时间。开发过程中只能新增样本或修正有书面理由的错误标签，不得删除失败样本来提高指标。

计分规则：

- 消息级 actionable 分类以“是否至少有一个有效结果”为正类、空 `results` 为负类，计算 precision 和 recall。
- 整体结果集按 `intent + target_reference + evidence_quote + kind + 规范化标题 + due_at + remind_at + missing_fields` 做无序一一匹配；规范化只移除首尾空白和约定标点，不使用另一个 AI 评分。存在遗漏、额外结果或字段错误时，该样本不算精确匹配。
- 单事项时间只有 `due_at` 和 `remind_at` 均与标签完全一致才算正确，不使用模糊时间容差。
- 更新或取消只有 intent 与唯一 `target_reference` 都正确才算关联正确。
- `clarify` 必须保留对应缺失字段为 `null` 并列入 `missing_fields`，任何隐藏默认日期或时间都判失败。

固定模型版本、提示词版本和采样参数后连续运行三次，三次都应达到：

- actionable 分类 precision 和 recall 均不低于 90%，无序结果集精确匹配率不低于 90%。
- 60 条明确时间样本中的绝对时间完全匹配率不低于 95%，即至少 57 条正确。
- 20 条上下文更新或取消样本中的目标关联准确率不低于 90%，即至少 18 条正确。
- 10 条多结果样本至少 9 条结果集完全匹配；10 条单项信息不完整样本全部不得擅自填充时间。

延迟在固定网络、固定模型、连接预热且不发生重试的条件下统计至少 30 个请求，从 inbox 提交到确认 outbox 创建的 p95 不超过 10 秒。报告同时记录 p50、p95、最大值、Token 和估算费用。

## 19. MVP 验收标准

- 只有来源白名单中的普通消息会进入识别 inbox 和 AI 流程，其他聊天正文不落库、不记日志且不上云；通过鉴权的 `kind=command` 可以独立进入优先命令 inbox。除用户显式启用后的 `修改` 新要求和目标最小摘要外，命令及控制会话上下文绝不上传 AI。
- 只有 `owner_qq_id` 能在指定的同账号自聊或“我的电脑”控制会话执行命令，未授权操作者无法判断任务是否存在。
- 程序不读取启动前或断线期间的历史消息，并清楚显示无法覆盖的离线区间。
- 达到持久化积压或数据库硬上限时进入可见的 `overloaded` 状态，不继续保存新来源正文，并准确报告漏识别区间。
- 已成功写入 inbox 且未超过隐私保留期的事件在程序重启后不会静默丢失；处于明确记录的离线或过载区间，以及进入 `expired_privacy` 的事件不作继续识别承诺。
- 同一 OneBot 事件只处理一次；一条消息可生成多个独立候选，但每个任务只存在一个当前候选版本。
- 候选被忽略、过期或取代后，其尚未提交的通知进入 `obsolete`，不会继续发给用户。
- 所有创建、修改和取消都必须经过所有者显式操作；AI 取消候选可通过版本化 `确认` 落地，直接 `取消` 命令本身视为显式操作；重复命令幂等，旧版本和过期候选不能生效。
- 已调度任务改期或确认取消时，只有目标仍处于允许状态才能原子修改；若目标已开始触发，候选进入 `conflict` 且不会额外创建新提醒。
- MVP 不创建周期任务，每个任务只有一个实际 `remind_at`。
- SQLite 是唯一事实来源，程序重启后所有非终态都能恢复或进入有原因的终态。
- Windows 休眠或程序停机后，任务按配置补发并标注延迟，或明确标记为 `missed`。
- NapCat 短暂断线后能够恢复连接，断线中的发送请求不会被误记为成功。
- 系统自己发送的确认和提醒不会进入命令解析或 AI 流程。
- 正常响应路径不会重复发送同一 delivery；结果未知时执行有限次数尽力重试，可能重复的消息具有相同任务 ID 和明确提示，耗尽后保留 `unknown_exhausted`。
- QQ 明确发送失败时任务和 outbox 不会静默丢失，并在达到配置上限前执行持久化重试；系统不承诺有限重试后必达。
- 取消成功后不再创建新的提醒发送意图；若发送已经开始，用户会收到“可能已发送”的结果。
- SQLite 暂时锁定或不可写时不会生成仅存在内存中的候选或任务。
- 原始正文、候选摘录、已完成发送载荷、动作响应、日志和备份按各自配置完成自动清理；摘录过期不影响未来任务发送。
- AI 固定评测集和延迟达到第 18.5 节指标。
- 在受支持 Windows 环境中完成安装、凭据配置、用户登录后启动、重启恢复和卸载验证。

## 20. 主要风险

### 20.1 同账号发送和鉴权限制

NapCat 是否能够稳定向登录账号自身或“我的电脑”发送、区分系统主动消息并鉴权用户回复，是当前最大的技术风险。必须在阶段 0 验证；任务 ID 不得被当作权限凭证。

### 20.2 QQ 和 NapCat 版本兼容

QQ 更新可能造成 NapCat 暂时失效或改变事件字段。程序将 NapCat 接入限制在独立网关模块，保存兼容性 fixture，并固定已验证版本。

### 20.3 AI 误判和提示注入

自然语言中的相对时间、上下文和恶意文本可能产生错误输出。通过固定 Schema、程序校验、目标 allowlist、版本化确认和冻结测试集共同降低风险。

### 20.4 提醒送达语义

Windows 休眠、QQ 离线、网络中断以及响应丢失都会影响提醒。首版提供持久化、补发、结果未知和有限次数尽力重试，但不承诺必达或严格恰好一次；结果未知时可能重复，耗尽后仍保留不确定终态。

### 20.5 隐私和云端数据

指定会话可能包含他人信息。系统必须最小化上传与保留内容，确保未授权会话不上云，并让日志、备份和清理遵循同一保留策略。启用前必须审查并记录供应商保留、训练和删除政策；本地删除不等于云端副本已经删除。

### 20.6 队列积压和云端成本

活跃群聊可能造成高请求量、上下文乱序和成本增长。通过内存有界队列、持久化积压与数据库硬上限、按会话串行、全局并发、速率限制、费用软上限和熔断控制风险。超过硬上限时会明确丢弃新来源消息并报告漏识别区间。

### 20.7 本地数据损坏和升级失败

异常断电、磁盘满或错误迁移可能损坏任务状态。使用 WAL、事务、迁移测试、一致性备份和失败后保留原库降低风险。

## 21. 后续扩展

完成 QQ 闭环并稳定运行后，可以按优先级增加：

1. 双 NapCat、第二 QQ 私聊或专用提醒群控制模式。
2. Windows 系统通知作为独立备用通道。
3. Windows 托盘管理界面。
4. 同一任务的多个提醒触发点和周期性提醒。
5. 图片和聊天截图 OCR。
6. 语音转文字后识别提醒。
7. 提醒冲突检测。
8. Meet 课表或其他日历写入适配器。
9. 多模型切换、离线模型和详细成本统计。

## 22. 下一步行动

下一步只实施阶段 0，不接入云端 AI 或正式调度器：

1. 编写最小 NapCat 探针，输出脱敏后的事件字段和动作响应。
2. 验证同账号自聊和“我的电脑”，确定唯一正式控制模式；两者都不满足鉴权与隐私要求时停止阶段 0。
3. 验证 Access Token、所有者字段、防循环字段、事件与响应顺序、重连和结果未知场景。
4. 将真实脱敏事件保存为自动化测试 fixture，并记录兼容版本。
5. 根据实测结果更新本计划中的目标标识、鉴权规则和送达能力边界。

只有阶段 0 的退出条件全部满足后，才进入阶段 1。
